require "spec_helper"

describe RooOnRails do
  it "has a version number" do
    expect(RooOnRails::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
